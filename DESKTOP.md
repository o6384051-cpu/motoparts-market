# تشغيل موتو بارتس ماركت على الكمبيوتر

هذا المشروع يحتوي على إصدار سطح مكتب يعمل من خلال متصفح الكمبيوتر، ولا يحتاج إلى APK أو Expo Cloud Build.

## المتطلبات

يحتاج الكمبيوتر إلى Node.js وpnpm. بعد نسخ المشروع إلى الكمبيوتر، نفّذ:

```bash
pnpm install
pnpm desktop
```

يفتح الخادم المحلي على `http://127.0.0.1:4173`. يمكن تغيير المنفذ عند الحاجة:

```bash
DESKTOP_PORT=5050 pnpm desktop
```

## تصدير الحزمة فقط

لإنشاء ملفات الويب الثابتة داخل `dist/` دون تشغيل الخادم:

```bash
pnpm export:web
```

بعد ذلك يمكن تشغيل المجلد `dist/` باستخدام أي خادم ملفات محلي يدعم مسارات SPA.

## العمل دون اتصال

بعد اكتمال التصدير، تكون ملفات الواجهة والأيقونة والكتالوج الأساسي محلية. يحفظ التطبيق الأصناف المضافة، الكميات، المبيعات، العملة، الأسعار، الخامات، الماركات، والسلة داخل تخزين المتصفح المحلي. يجب تشغيل الخادم المحلي مرة واحدة في كل جلسة تشغيل؛ ولا يلزم اتصال بالإنترنت.

## الدخول

يفتح التطبيق شاشة الدخول المحلية. رمز الدخول هو:

```text
osamakamel1121978
```

## ملاحظة البيانات

التخزين المحلي مرتبط بمتصفح الكمبيوتر وملفه الشخصي. يُنصح بإضافة تصدير واستيراد نسخة احتياطية قبل نقل التطبيق إلى كمبيوتر آخر أو حذف بيانات المتصفح.
